import { ExcludeColsPipe } from './exclude-cols.pipe';

describe('ExcludeColsPipe', () => {
  it('create an instance', () => {
    const pipe = new ExcludeColsPipe();
    expect(pipe).toBeTruthy();
  });
});
